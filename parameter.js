function startin(student) {
    console.log('this is my book ');
    var countin = 80;
    var count = countin / student;
    return count;
}

person = 8;
var tearm = startin(person);
console.log(tearm);